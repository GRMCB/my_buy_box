version: 0.0
os: linux 
files:
  - source: /
    destination: /var/www/webapp
hooks:
  AfterInstall:
    - location: src/scripts/install_dependencies.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: src/scripts/start_service.sh
      timeout: 120
      runas: root
